#include "main.h"
/**
 * _abs - compute absolute value of integer
 * @i: input variable
 * Return: absolute input value
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
