#include "main.h"
/**
 * _abs - compute absolute value of integer
 * @i: input variable
 * Return: absolute input value
 */
int _abs(int i)
{
	if (i > 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
