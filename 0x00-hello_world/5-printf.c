/* use printf, no puts */
#include <stdio.h>
/**
 * main - program entry point
 * Return: value of 0 indicates program runs successfully
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
